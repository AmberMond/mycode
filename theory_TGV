## 初代——无黏欧拉求解器（处理稳态问题）——2D bump


## TGV 可压求解器模拟不可压NS测试
* initialization using exact solution (u, v, p)

$$u(x, y, t=0) = U_0 * sin(x)cos(y)$$
$$v(x, y, t=0) = U_0 * cos(x)sin(y)$$
$$p(x, y, t=0) = p_0 + \frac{\rho U_0^2}{4}cos(2x)cos(2y)$$


low mach number so the solver can deal with this imcompressible case like $Ma \approx 0.003$


however the boundary should be updated according to physical time

* change JFNK from steady to unsteady

including physical time and newton-iterition

* add viscous term and update the way of calculating residuals

set gradient calculation part

based on 
$$\nabla \Phi = \frac{1}{V_{cell}} \Sigma\Phi\vec{n}S_f$$
